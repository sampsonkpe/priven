import PageNav from "../components/PageNav";

const Blessings = () => (
  <div className="grain-bg bg-background min-h-screen">
    <PageNav />
    <main className="px-6 pt-32 pb-24 max-w-lg mx-auto text-center uppercase">
      <h1 className="font-serif text-2xl sm:text-3xl tracking-[0.15em] text-foreground">
        Blessings
      </h1>
    </main>
  </div>
);

export default Blessings;